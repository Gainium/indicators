# 001 — Wire mocha and CI to a repo with no tests

## Build order

1. Add devDependencies: `mocha`, `chai`, `@types/mocha`, `@types/chai`
   (`ts-node` already present).
2. Add `test/placeholder.spec.ts`.
3. Replace the failing default `"test"` script with a real mocha
   invocation over `test/**/*.spec.ts` via `ts-node/register`.
4. Add `.github/workflows/test.yml`: PR + push to `main` (default
   branch), Node 22, `npm ci`, `npm test`.
5. Verify `npm test` green locally.

## Blast radius

1. Other call sites: none — new file. The replaced `"test"` script was
   never invoked elsewhere in this repo.
2. `core/` submodule: none in this repo.
3. CLAUDE.md contracts: no indicator math touched — devDependency/CI/test
   file changes only. (Prior work in this repo validated indicator
   parity with TradingView Pine Script; nothing here revisits that.)
4. Other tests: none exist yet.

## Traceability

| Spec section | Test / file |
|---|---|
| §2.1/§2.3 | `test/placeholder.spec.ts`, `package.json` `"test"` |
| §2.2 | `.github/workflows/test.yml` |
